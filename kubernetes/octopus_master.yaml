apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: octopus
spec:
  # serviceName: octopus-service
  replicas: 1
  selector:
    matchLabels:
      app: octopus
  template:
    metadata:
      labels:
        app: octopus
    spec:
      # volumes:
      # - name: storage-name
      #   persistentVolumeClaim:
      #    claimName: pvc-name
      containers:
      - name: octopus
        image: octopus:latest
        args: ["--mode=master", "--distributionFactor=1"]
        imagePullPolicy: "Always"
        # volumeMounts:
        # - mountPath: "/data"
        #   name: storage-name
        ports:
        - containerPort: 50051
          name: master
